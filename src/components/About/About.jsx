import { styles } from "../../styles";

const About = () => {
  return (
    <div className={`${styles.padding}`}>
      <p className={styles.microText}>About me</p>
    </div>
  );
};

export default About;
